#include <iostream>
#include <string>

using namespace std;

int main() {
    int age, standard;
    string first_name, last_name;

    cin >> age;
    cin >> first_name;
    cin >> last_name;
    cin >> standard;

    cout << age << " " << first_name << " " << last_name << " " << standard << endl;

    return 0;
}
